import { NotFound } from '@/components/ui/not-found';

export default function NotFoundPage() {
    return (
        <NotFound description="Oops! The short URL doesn't exist or is corrupted" />
    );
}
